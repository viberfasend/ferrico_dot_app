name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump (empty = auto-detect from commits)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  prepare:
    name: Prepare release
    runs-on: blacksmith-4vcpu-ubuntu-2404
    outputs:
      version: ${{ steps.calc.outputs.version }}
      tag: ${{ steps.calc.outputs.tag }}
      changelog: ${{ steps.calc.outputs.changelog }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Calculate version and changelog
        id: calc
        env:
          BUMP_OVERRIDE: ${{ inputs.bump }}
        run: |
          python3 - <<'PYEOF'
          import subprocess, re, json, os
          from pathlib import Path

          try:
              last_tag = subprocess.check_output(['git', 'describe', '--tags', '--abbrev=0']).decode().strip()
              range_spec = f'{last_tag}..HEAD'
          except subprocess.CalledProcessError:
              last_tag = ''
              range_spec = 'HEAD'

          raw = subprocess.check_output(['git', 'log', range_spec, '--pretty=format:%s']).decode().strip()
          commits = [c for c in raw.split('\n') if c and not c.startswith('Merge ')]

          if not commits:
              print("No commits since last release")
              raise SystemExit(1)

          bump_override = os.environ.get('BUMP_OVERRIDE', '').strip()
          bump = 'patch'
          for c in commits:
              if 'BREAKING CHANGE' in c or re.match(r'^[a-z]+(\(.+\))?!:', c):
                  bump = 'major'; break
              if re.match(r'^feat(\(.+\))?:', c):
                  bump = 'minor'
          if bump_override in ('major', 'minor', 'patch'):
              bump = bump_override

          pkg = json.loads(Path('package.json').read_text())
          major, minor, patch = map(int, pkg['version'].split('.'))
          if bump == 'major':   new_ver = f'{major+1}.0.0'
          elif bump == 'minor': new_ver = f'{major}.{minor+1}.0'
          else:                 new_ver = f'{major}.{minor}.{patch+1}'

          breaking, feats, fixes, perfs = [], [], [], []
          for c in commits:
              if 'BREAKING CHANGE' in c or re.match(r'^[a-z]+(\(.+\))?!:', c):
                  breaking.append(re.sub(r'^[a-z]+(\(.+\))?!?:\s*', '', c))
              elif re.match(r'^feat(\(.+\))?:', c):
                  feats.append(re.sub(r'^feat(\(.+\))?: ', '', c))
              elif re.match(r'^fix(\(.+\))?:', c):
                  fixes.append(re.sub(r'^fix(\(.+\))?: ', '', c))
              elif re.match(r'^perf(\(.+\))?:', c):
                  perfs.append(re.sub(r'^perf(\(.+\))?: ', '', c))

          parts = []
          if breaking: parts.append('### Breaking Changes\n\n' + '\n'.join(f'- {x}' for x in breaking))
          if feats:    parts.append('### Features\n\n'          + '\n'.join(f'- {x}' for x in feats))
          if fixes:    parts.append('### Bug Fixes\n\n'          + '\n'.join(f'- {x}' for x in fixes))
          if perfs:    parts.append('### Performance\n\n'        + '\n'.join(f'- {x}' for x in perfs))
          changelog = '\n\n'.join(parts)

          gho = os.environ['GITHUB_OUTPUT']
          with open(gho, 'a') as f:
              f.write(f'version={new_ver}\n')
              f.write(f'tag=v{new_ver}\n')
              delim = 'CHANGELOG_HEREDOC_EOF'
              f.write(f'changelog<<{delim}\n{changelog}\n{delim}\n')

          print(f'Bump: {bump} → {new_ver}')
          PYEOF

      - name: Update package.json version
        env:
          VERSION: ${{ steps.calc.outputs.version }}
        run: |
          python3 - <<'PYEOF'
          import json, os
          from pathlib import Path
          p = Path('package.json')
          pkg = json.loads(p.read_text())
          pkg['version'] = os.environ['VERSION']
          p.write_text(json.dumps(pkg, indent=2) + '\n')
          PYEOF

      - name: Update tauri.conf.json version
        env:
          VERSION: ${{ steps.calc.outputs.version }}
        run: |
          python3 - <<'PYEOF'
          import json, os
          from pathlib import Path
          p = Path('src-tauri/tauri.conf.json')
          conf = json.loads(p.read_text())
          conf['version'] = os.environ['VERSION']
          p.write_text(json.dumps(conf, indent=2) + '\n')
          PYEOF

      - name: Update Cargo.toml version
        env:
          VERSION: ${{ steps.calc.outputs.version }}
        run: |
          sed -i "0,/^version = .*/s//version = \"$VERSION\"/" src-tauri/Cargo.toml

      - name: Update CHANGELOG.md
        env:
          VERSION: ${{ steps.calc.outputs.version }}
          CHANGELOG: ${{ steps.calc.outputs.changelog }}
        run: |
          python3 - <<'PYEOF'
          import os
          from pathlib import Path
          from datetime import date

          version = os.environ['VERSION']
          changelog = os.environ['CHANGELOG']
          today = date.today().isoformat()

          p = Path('CHANGELOG.md')
          content = p.read_text()
          lines = content.split('\n')

          new_entry = f'## [{version}] - {today}\n\n{changelog}\n'
          # Insert after the first line (the # Changelog heading)
          lines.insert(1, '\n' + new_entry)
          p.write_text('\n'.join(lines))
          PYEOF

      - name: Commit and tag
        env:
          VERSION: ${{ steps.calc.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml CHANGELOG.md
          git commit -m "chore(release): v$VERSION"
          git tag "v$VERSION"
          git push origin HEAD:main
          git push origin "v$VERSION"

  build:
    name: Build (${{ matrix.artifact-name }})
    needs: prepare
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: blacksmith-4vcpu-ubuntu-2204
            artifact-name: linux
            rust-targets: ''
            tauri-args: ''
          - platform: blacksmith-4vcpu-windows-2025
            artifact-name: windows
            rust-targets: ''
            tauri-args: ''
          - platform: blacksmith-6vcpu-macos-15
            artifact-name: macos
            rust-targets: aarch64-apple-darwin,x86_64-apple-darwin
            tauri-args: --target universal-apple-darwin

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - uses: oven-sh/setup-bun@v2

      - name: Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxdo-dev \
            pkg-config \
            build-essential

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - uses: Swatinem/rust-cache@v2
        with:
          workdir: src-tauri

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Tauri app
        shell: bash
        env:
          TAURI_ARGS: ${{ matrix.tauri-args }}
        run: |
          set -euo pipefail
          echo "Running: bun run tauri build $TAURI_ARGS"
          bun run tauri build $TAURI_ARGS

      - name: List bundle output (debug)
        shell: bash
        run: |
          echo "--- src-tauri/target top-level ---"
          ls -la src-tauri/target || true
          echo "--- all 'bundle' directories ---"
          find src-tauri/target -type d -name bundle -print 2>/dev/null || true
          echo "--- files inside bundle directories ---"
          find src-tauri/target -type d -name bundle -exec find {} -type f \; 2>/dev/null || true

      - name: Stage bundle artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p staged-artifacts
          # Copy every release artifact produced by `tauri build` into a flat dir.
          # Covers Linux (deb/rpm/AppImage), Windows (msi/exe), macOS (dmg/app.tar.gz/sig),
          # and the Tauri updater signature files.
          find src-tauri/target -type d -name bundle | while read -r d; do
            find "$d" -type f \( \
              -name '*.deb' -o -name '*.rpm' -o -name '*.AppImage' \
              -o -name '*.msi' -o -name '*.exe' \
              -o -name '*.dmg' -o -name '*.app.tar.gz' -o -name '*.app.tar.gz.sig' \
              -o -name '*.tar.gz' -o -name '*.zip' -o -name '*.sig' \
            \) -exec cp -v {} staged-artifacts/ \;
          done
          echo "--- staged artifacts ---"
          ls -la staged-artifacts/
          if [ -z "$(ls -A staged-artifacts/)" ]; then
            echo "::error::No bundle artifacts were produced by 'tauri build'."
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.artifact-name }}
          path: staged-artifacts/*
          if-no-files-found: error
          retention-days: 1

  # Android is its own job rather than a matrix entry: it needs a JDK, the
  # Android SDK/NDK and cross-compilation targets, none of which the desktop
  # platforms use.
  #
  # If this job fails the run goes red — but the `release` job below still
  # publishes (see its `if:`). `prepare` has already pushed the tag by then, so
  # letting a mobile-only hiccup abort the workflow would strand a tag with no
  # release at all. Shipping without an APK is recoverable; a stranded tag is
  # not. The red run is deliberate: a missing APK should be loud, not silent.
  android:
    name: Build (android)
    needs: prepare
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # Use the runner's preinstalled NDK rather than downloading ~1GB. If a
      # future NDK breaks the rusqlite `cc` cross-compile (the classic failure
      # mode — see docs/mobile-v1-playbook.md), pin a known-good one here with
      # `sdkmanager --install "ndk;<version>"` and point NDK_HOME at it.
      - name: Resolve NDK
        run: |
          echo "NDK_HOME=$ANDROID_NDK_LATEST_HOME" >> "$GITHUB_ENV"
          echo "Using NDK at $ANDROID_NDK_LATEST_HOME"

      - uses: dtolnay/rust-toolchain@stable

      - name: Add Rust Android targets
        run: rustup target add aarch64-linux-android armv7-linux-androideabi i686-linux-android x86_64-linux-android

      - uses: Swatinem/rust-cache@v2
        with:
          workdir: src-tauri
          # Cross-compiled artifacts don't share a cache with the host build.
          key: android-release

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('src-tauri/gen/android/**/*.gradle*') }}
          restore-keys: gradle-${{ runner.os }}-

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Release signing (#74): the keystore lives in repo secrets and is
      # materialized as the gitignored `keystore.properties` + keystore file
      # that app/build.gradle.kts picks up. A *stable* key matters — the old
      # debug-signed builds used a keystore generated fresh on every runner,
      # so each release had a different signature and Android refused to
      # install it over the previous version.
      #
      # Fail loudly if the secrets are missing: an unsigned release APK is
      # uninstallable, and the `release` job below ships desktop artifacts
      # even when this job goes red.
      - name: Set up release keystore
        env:
          ANDROID_KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          if [ -z "${ANDROID_KEYSTORE_B64}" ] || [ -z "${ANDROID_KEYSTORE_PASSWORD}" ] \
            || [ -z "${ANDROID_KEY_ALIAS}" ] || [ -z "${ANDROID_KEY_PASSWORD}" ]; then
            echo "::error::Android signing secrets missing (ANDROID_KEYSTORE_B64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD). See docs/android-release-signing.md."
            exit 1
          fi
          app_dir=src-tauri/gen/android/app
          printf '%s' "$ANDROID_KEYSTORE_B64" | base64 -d > "$app_dir/upload.keystore"
          cat > "$app_dir/keystore.properties" <<EOF
          storeFile=upload.keystore
          storePassword=$ANDROID_KEYSTORE_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          keyPassword=$ANDROID_KEY_PASSWORD
          EOF

      # Real `--release` build now that signing works: minified, stripped
      # native libs. `--split-per-abi` emits one APK per ABI instead of a
      # universal APK bundling every architecture — the debug universal APK
      # weighed ~1GB (unstripped debug .so files x 4 ABIs); a stripped
      # per-ABI release APK is a few dozen MB. Only arm targets: x86/x86_64
      # are emulator-only and nobody sideloads those from a GitHub release.
      - name: Build APK
        run: bun run android:build -- --apk --split-per-abi --target aarch64 armv7

      - name: Stage APK
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p staged-artifacts
          found=0
          while IFS= read -r apk; do
            cp -v "$apk" "staged-artifacts/ferrico_${VERSION}_android_$(basename "$apk")"
            found=1
          done < <(find src-tauri/gen/android -name '*.apk' -type f)
          if [ "$found" -eq 0 ]; then
            echo "::error::No APK produced by 'tauri android build'."
            find src-tauri/gen/android -name '*.apk' -o -name '*.aab' || true
            exit 1
          fi
          ls -la staged-artifacts/

      - uses: actions/upload-artifact@v4
        with:
          # `binaries-*` is what the release job globs for, so naming it this
          # way is all that's needed to get the APK attached to the release.
          name: binaries-android
          path: staged-artifacts/*
          if-no-files-found: error
          retention-days: 1

  release:
    name: Create GitHub release
    needs: [prepare, build, android]
    # Publish as long as the desktop bundles are there. `!cancelled()` overrides
    # the default "skip if a dependency failed", so a failed `android` job does
    # not block the release — the desktop artifacts still ship and the APK is
    # simply absent from the assets.
    if: ${{ !cancelled() && needs.prepare.result == 'success' && needs.build.result == 'success' }}
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - uses: actions/download-artifact@v4
        with:
          pattern: binaries-*
          path: release-artifacts
          merge-multiple: true

      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare.outputs.tag }}
          CHANGELOG: ${{ needs.prepare.outputs.changelog }}
        run: |
          printf '%s' "$CHANGELOG" > release-notes.md
          readarray -t FILES < <(find release-artifacts -type f | sort)
          gh release create "$TAG" \
            --title "Release $TAG" \
            --notes-file release-notes.md \
            "${FILES[@]}"
